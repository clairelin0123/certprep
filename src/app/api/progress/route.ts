import { NextResponse } from "next/server";

// Placeholder for future user progress tracking
export async function GET() {
  return NextResponse.json({
    message: "Progress tracking coming soon",
  });
}

export async function POST() {
  return NextResponse.json({
    message: "Progress tracking coming soon",
  });
}
